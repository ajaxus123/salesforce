<h2>Add a New Store</h2>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@store) do |f| %>
        
        <%= f.label :storename %>
        <%= f.text_field :storename, class: 'form-control' %>

        <%= f.label :storecode %>
        <%= f.text_field :storecode, class: 'form-control' %>

        <%= f.label :storename %>
        <%= f.text_field :storename, class: 'form-control' %>

        <%= f.label :storename %>
        <%= f.text_field :storename, class: 'form-control' %>

        <%= f.submit "Add Store", class: "btn btn-primary" %>

        <% end %>
    </div>
</div>